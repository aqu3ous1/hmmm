// Bumped on every ship. The title screen and the console both show this, so a
// stale cache is identifiable at a glance rather than by guesswork.
export const BUILD = '7';
export const BUILD_NAME = 'third person';
export const BUILD_DATE = '2026-08-21';
