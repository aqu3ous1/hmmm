// Bumped whenever something ships. Shown on the title screen and logged to the
// console at boot, so "am I actually looking at the new build?" is a question
// you can answer in one glance instead of guessing at the cache.
export const BUILD = '2';
export const BUILD_NAME = 'graphics overhaul';
export const BUILD_DATE = '2026-08-13';
